# Barbar Cafe — сортировка и поиск над вкладками, 25 сентября 2026

Статус: публикуется в `origin`, ветка `barbar`, production `https://barbar-cafe.netlify.app` (Netlify собирает по push). Совпадение ассетов на проде проверяется после push и указывается в отчёте о выпуске.

База выпуска: `c42f803` (единый календарь дат). `.env*`, `.claude`, `.agents`, локальные данные и артефакты тестов в коммит не входят.

## Что выпускается

- В каталогах сортировка и поиск стоят одной строкой над вкладками категорий, у правого края; вкладки занимают строку под ними. Разметка: общий блок `.catalog-actions` внутри `.catalog-tools`, перед `CategoryTabs`/`InventoryCategories`.
- Где: продажи (`SalesCatalog`, обе роли, день и заказ), склад владельца и сотрудника (вместе с переключателем вида), рецепты на телефоне (`Cocktails`, `StaffRecipes`). На компьютере рецепты не менялись: сортировка и поиск там уже справа в заголовке над вкладками.
- На телефоне (до 760px) сверху строка «поиск + кнопка „Сортировка и фильтры“», ниже выпадающий список категорий и подсказка группы склада.
- Удалены нерабочие правила `.sales-catalog-tools` (сетка с `order` не срабатывала из-за обёртки `div.category-tabs`) и устаревшие ширины `.catalog-tools .search` в `_responsive.scss` и `_fluid-layout.scss`.
- Логика сортировки, поиска, категорий и сохранения фильтров в сессии не менялась. Миграций и изменений API нет.

## Проверки до публикации

По указанию владельца («деплой на прод без тестов») автоматические тесты **не запускались**: `npm test`, `npm run test:e2e`, `test:db`, `test:backup`, `test:scheduled-backup`. Выполнена только сборка для публикации `npm run build` (ESLint `--max-warnings 0`, photo manifest, `tsc -b`, Vite): прошла. `dist/index.html` ссылается на `assets/main-dKuFwW4Z.js` и `assets/main-DLoop40I.css`; в этом CSS есть `.catalog-actions`.

Состояние прода перед push (25.09.2026): `/` — 200 (ассеты `main-CYqiwLg5.js`, `main-Cf8Gmh_p.css`), `/api/menu` — 200, `/menu` — **500**.

## Ограничения и ручная проверка

- Не проверено автоматически: раскладка на промежуточных ширинах (761–1100px, свёрнутое боковое меню, полноэкранный режим продаж) и на телефоне.
- Владельцу проверить вручную: продажи (день и заказ), склад, склад сотрудника, рецепты на телефоне — действия справа над вкладками, кнопка фильтров открывает шторку.
- `https://barbar-cafe.netlify.app/menu` отдаёт HTTP 500 после выпуска `1438c29` (перед этим выпуском проверено: 500). Этот выпуск меняет только разметку и стили каталога, серверные функции не трогает и 500 не чинит. Нужен отдельный разбор логов функции `barbar-menu-page` в панели Netlify.
- Календарь из прошлого выпуска (вложенные окна, iOS Safari, 390px) вручную на проде ещё не проверен.
