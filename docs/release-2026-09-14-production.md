# Production — 14 сентября 2026

По явному разрешению владельца ветка `barbar` отправлена в `decoder10/barbar`. Netlify опубликовал коммит `0c837317bd4df518395ffe1db24d5f583b1721c9` в 16:18 по Еревану.

- Сайт: https://barbar-cafe.netlify.app/
- Deploy: https://app.netlify.com/projects/barbar-cafe/deploys/6aa7e5ff22a6560008cd76ad
- На домене проверен `/assets/index-ByWRHFoN.js`, совпадающий с проверенной локальной сборкой.

## Выпущено

Все десять коммитов после `90f1cde`: сохранение фильтров, пользовательские роли и операции склада, серверные отчёты и пагинация, AVIF/WebP и кеширование, индексы MongoDB, защита действий от повторных нажатий, автоматическое раскрытие каталога при прокрутке, сброс прокрутки при переходах, полноэкранные продажи, предупреждения об остатках и Web Push. Календарь и кнопка полного экрана находятся в одной строке.

## Проверки

- Перед выпуском: ESLint/TypeScript, 82 модульных теста, production build, 46 браузерных сценариев, 23 проверки MongoDB в отдельной тестовой базе. Зашифрованное резервирование и восстановление проверены на 502 тестовых документах.
- Production: HTML 200; анонимный auth возвращает `authenticated:false`, данные и Push API отвечают 401, API использует `no-store`.
- AVIF: `image/avif` и `public,max-age=31536000,immutable`. Service worker: JavaScript 200 и `no-cache`; manifest доступен.
- Открыта существующая сессия владельца Грача; настройки уведомлений успешно получили серверную конфигурацию и разрешили кнопку подписки.
- Netlify показывает 9 production functions; `barbar-push-retry` зарегистрирована как Scheduled, каждые 5 минут.
- Три переменные BARBAR_PUSH_* сохранены только для Production. Приватный ключ Secret; значения не включены в Git. На бесплатном тарифе scopes ограничены доступными настройками Netlify.
- Тестовые продажи, сбросы и подмена рабочих данных не выполнялись.

## Что остаётся

Разрешение системных уведомлений на устройстве и реальная доставка ещё не подтверждены: при подключении устройства Mac оказался заблокирован. На iOS приложение нужно добавить на экран «Домой». Уведомления привязаны к действующему сеансу; подробнее в [описании Push](stock-notifications.md).

Отдельно остаются рабочая зашифрованная копия БД, замена раскрытого пароля Atlas и удаление Production URI из тестовых контекстов. Эти операции ранее требовали отдельного подтверждения/ручного ввода и не завершены этим выпуском. См. [облачный аудит](cloud-security.md).

Следующий приоритет: завершить эти настройки БД, затем наблюдать реальные задержки и ошибки функций; для дальнейшего развития — партии заготовок с FEFO и более точный прогноз спроса с учётом отсутствия товара. Чеки, столы, оплата и закрытие смены остаются отложенными по просьбе пользователя.
