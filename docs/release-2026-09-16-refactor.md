# Barbar Cafe — рефакторинг, панель уведомлений и ускорение, 16 сентября 2026

Опубликованы коммиты `5025632` и `b6d3ac7`, ветка `barbar`. Содержание и замеры: [refactor-2026-09-16.md](refactor-2026-09-16.md).

## Проверки перед публикацией

- `npm run check`: ESLint, TypeScript, 164 модульных теста.
- `npm run test:db` в отдельной локальной MongoDB: 36 проверок.
- `npm run test:load`: 50 000 продаж — история по курсору 51 документ/51 ключ, карточки «сначала продаваемые» 30,4 мс холодно / 3,95 мс из кеша, целостность остатков и аудита подтверждена.
- `npm run build`: чанки `react` 216,5 / 67,3 КБ gzip, `main` 142 / 39, `photo-manifest` 98,5 / 21,7, `photo-catalog` 27,9 / 10,2; 24 файла шрифтов (220 КБ); `dist/barbar` 24 МБ (единственный JPG — фон входа).
- Playwright: 56 сценариев, включая панель уведомлений, объединённый запрос карточек, изоляцию фильтров при выходе.
- `npm run test:backup` и `npm run test:scheduled-backup`: пройдены после повторного запуска Docker. Первый прогон упал с `Topology is closed` / `ECONNREFUSED` — демон Docker остановился между проверками; на код это не указывает, но пуш к тому моменту уже был отправлен. Код резервирования в выпуске не менялся.

## Публикация и production

- `/` отдаёт ассеты локальной сборки: `main-CPDJLPxy.js`, `react-YWPnaJPF.js`, `photo-manifest-DE3Xnw6V.js`, `photo-catalog-CaKvs5ca.js`.
- `/` и `/menu` — 200; анонимные `/api/barbar`, `/api/barbar/users`, `/api/barbar/notifications` — 401; `/api/menu` — 200.
- Заголовки: `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload` (Netlify добавил `preload`), CSP прежняя.
- `npm run probe:latency`: `/api/menu` — медиана 72 мс, первый ответ 526 мс; `/api/barbar/auth` — медиана 231 мс. Сопоставимо с выпуском утра (71 / 524; 233).
- Не проверено на production: панель уведомлений и лента под реальным входом (нужен вход владельца; продажи и закупки не создавались), доставка push на устройства, планы запросов на Atlas. Кеш популярности и объединённый запрос карточек подтверждены только локально.

## Что осталось

См. раздел «Рефакторинг 16 сентября» в [next-work.md](next-work.md).
